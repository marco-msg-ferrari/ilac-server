{% extends '::base.html.twig' %}
{% block body %}
  <h1>{{ title }}</h1>
  <div>{{ lastTest.getValue }}  {{ lastTest.getCreatedAt|date("Y/m/d H:i") }}</div>
<div>
  {{ form_start(form) }}
  {{ form_widget(form) }}
  {{ form_end(form) }}
</div>
{%  endblock %}
